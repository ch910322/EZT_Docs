//<halrule>server-only</halrule>
begin
  string 255 bd;
  bd = left(VersionBuild,4);
  if (bd=="8533") then begin
    SourceFile("halcust/EZT_mods/ExternalDocs/8533/hal.hob",0);
  end;
  return;
end;
